{{#each this}}
<li class="card-list">
    <div class="card">
        <img class="card-photo-img" src="{{images.6.url}}" alt=""/>

        <div class="card-stats">
            <p class="name-icons">
                {{name}}
            </p>
            <p class="date-icons">
                {{dates.start.localDate}}
            </p>
            <p class="city-icons">
                {{_embedded.venues.0.name}}
            </p>            
        </div>
    </div>
</li>
{{/each}}